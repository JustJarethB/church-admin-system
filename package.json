{
    "name": "nextjs-project",
    "version": "0.0.0",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "npm --prefix packages/frontend run build"
    }
}